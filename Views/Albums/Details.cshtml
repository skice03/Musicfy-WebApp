@model MusicfyWebApp.Models.Album

@{
    ViewData["Title"] = Model.Title;
}

<div class="container mt-5">
    <a asp-action="Index" class="text-secondary text-decoration-none mb-4 d-inline-block">&larr; Back to Albums</a>

    <div class="row mb-5 align-items-center">
        <div class="col-md-3 text-center mb-4 mb-md-0">
            @if (!string.IsNullOrEmpty(Model.CoverImageUrl))
            {
                <img src="@Model.CoverImageUrl" class="rounded shadow"
                     style="width: 250px; height: 250px; object-fit: cover;" alt="@Model.Title">
            }
            else
            {
                <div class="bg-secondary rounded shadow d-flex align-items-center justify-content-center mx-auto"
                     style="width: 250px; height: 250px;">
                    <span class="display-1 text-white-50">💿</span>
                </div>
            }
        </div>
        <div class="col-md-9">
            <h6 class="text-uppercase mb-2" style="color: #1DB954; font-weight: 600; letter-spacing: 2px;">Album</h6>
            <h1 class="display-3 fw-bold mb-3" style="color: white;">@Model.Title</h1>
            <p class="lead" style="color: #B3B3B3;">
                <a asp-controller="Artists" asp-action="Details" asp-route-id="@Model.ArtistId"
                   class="text-decoration-none" style="color: #1DB954;">
                    @(Model.Artist != null ? Model.Artist.Name : "Unknown Artist")
                </a>
                &middot; @Model.ReleaseYear
            </p>

            @if (User.IsInRole("Admin"))
            {
                <div class="mt-3">
                    <a asp-action="Edit" asp-route-id="@Model.AlbumId" class="btn btn-outline-secondary btn-sm me-2">Edit</a>
                    <a asp-action="Delete" asp-route-id="@Model.AlbumId" class="btn btn-outline-danger btn-sm">Delete</a>
                </div>
            }
        </div>
    </div>

    <hr style="border-color: #282828; margin-bottom: 40px;">

    <h3 class="mb-4 text-white" style="font-weight: 600;">Tracklist</h3>

    <table class="table table-dark table-hover mt-3" style="background-color: transparent;">
        <thead>
            <tr style="border-bottom: 2px solid #282828;">
                <th class="text-secondary pb-3">#</th>
                <th class="text-secondary pb-3">Title</th>
                <th class="text-secondary pb-3">Artist</th>
                <th class="text-secondary pb-3">Length</th>
                <th class="text-secondary pb-3"></th>
            </tr>
        </thead>
        <tbody>
            @{
                int index = 1;
            }

            @if (Model.Songs != null && Model.Songs.Any())
            {
                @foreach (var song in Model.Songs)
                {
                    <tr>
                        <td class="pt-3 text-secondary">@(index++)</td>
                        <td class="pt-3 fw-bold text-white">@song.Title</td>
                        <td class="pt-3">
                            <a asp-controller="Artists" asp-action="Details" asp-route-id="@song.ArtistId" class="text-decoration-none" style="color: #1DB954; transition: 0.2s;" onmouseover="this.style.textDecoration='underline'" onmouseout="this.style.textDecoration='none'">
                                @(song.Artist != null ? song.Artist.Name : Model.Artist?.Name ?? "Unknown")
                            </a>
                        </td>
                        <td class="pt-3" style="color: #B3B3B3;">@song.Length</td>
                        <td>
                            @if (!string.IsNullOrEmpty(song.AudioUrl))
                            {
                                <button class="btn btn-sm btn-outline-success btn-play-song" style="border-radius: 50px;"
                                        data-audio-url="@song.AudioUrl"
                                        data-song-title="@song.Title"
                                        data-song-artist="@(song.Artist?.Name ?? Model.Artist?.Name ?? "Unknown")">
                                    ▶ Play
                                </button>
                            }
                            else
                            {
                                <button class="btn btn-sm btn-outline-secondary" style="border-radius: 50px;" disabled>No Audio</button>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="5" class="text-center text-secondary py-4">No songs in this album yet.</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Mini Audio Player Bar -->
<div id="audioPlayerBar" class="fixed-bottom d-none" style="background: linear-gradient(180deg, #181818 0%, #000 100%); border-top: 1px solid #282828; padding: 10px 0;">
    <div class="container d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center">
            <div class="me-3">
                <span id="playerSongTitle" class="fw-bold text-white d-block" style="font-size: 0.9rem;">Song Title</span>
                <span id="playerSongArtist" class="text-secondary" style="font-size: 0.75rem;">Artist</span>
            </div>
        </div>
        <div class="flex-grow-1 mx-4">
            <audio id="audioPlayer" class="w-100" style="height: 32px; filter: sepia(20%) saturate(70%) grayscale(1) contrast(99%) invert(12%);"></audio>
        </div>
        <button id="closePlayer" class="btn btn-sm btn-outline-secondary" style="border-radius: 50px;">✕</button>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $(".btn-play-song").on("click", function () {
                var audioUrl = $(this).data("audio-url");
                var title = $(this).data("song-title");
                var artist = $(this).data("song-artist");

                $("#playerSongTitle").text(title);
                $("#playerSongArtist").text(artist);

                var player = document.getElementById("audioPlayer");
                player.src = audioUrl;
                player.controls = true;
                player.play();

                $("#audioPlayerBar").removeClass("d-none");
            });

            $("#closePlayer").on("click", function () {
                var player = document.getElementById("audioPlayer");
                player.pause();
                player.src = "";
                $("#audioPlayerBar").addClass("d-none");
            });
        });
    </script>
}